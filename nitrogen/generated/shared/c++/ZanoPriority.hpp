///
/// ZanoPriority.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

#pragma once

#include <cmath>
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::zano {

  /**
   * An enum which can be represented as a JavaScript enum (ZanoPriority).
   */
  enum class ZanoPriority {
    DEFAULT      SWIFT_NAME(default) = 0,
    UNIMPORTANT      SWIFT_NAME(unimportant) = 1,
    NORMAL      SWIFT_NAME(normal) = 2,
    ELEVATED      SWIFT_NAME(elevated) = 3,
    URGENT      SWIFT_NAME(urgent) = 4,
  } CLOSED_ENUM;

} // namespace margelo::nitro::zano

namespace margelo::nitro {

  using namespace margelo::nitro::zano;

  // C++ ZanoPriority <> JS ZanoPriority (enum)
  template <>
  struct JSIConverter<ZanoPriority> final {
    static inline ZanoPriority fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      int enumValue = JSIConverter<int>::fromJSI(runtime, arg);
      return static_cast<ZanoPriority>(enumValue);
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, ZanoPriority arg) {
      int enumValue = static_cast<int>(arg);
      return JSIConverter<int>::toJSI(runtime, enumValue);
    }
    static inline bool canConvert(jsi::Runtime&, const jsi::Value& value) {
      if (!value.isNumber()) {
        return false;
      }
      double integer;
      double fraction = modf(value.getNumber(), &integer);
      if (fraction != 0.0) {
        // It is some kind of floating point number - our enums are ints.
        return false;
      }
      // Check if we are within the bounds of the enum.
      return integer >= 0 && integer <= 4;
    }
  };

} // namespace margelo::nitro
